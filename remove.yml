- name: install
  hosts: localhost
  connection: local
  roles:
    - mozdev
  tasks:
    - name: remove
      file:
        path: "{{ profile_path }}/extensions/{{ manifest.applications.gecko.id }}"
        state: absent

